#include <string>
#include <vector>

using namespace std;

vector<int> solution(int n, vector<vector<int>> paths, vector<int> gates, vector<int> summits) {
    vector<int> answer;

    for (auto path : paths) {
        int i = path[0];
        int j = path[1];
        int w = path[2];

        
    }
    return answer;
}