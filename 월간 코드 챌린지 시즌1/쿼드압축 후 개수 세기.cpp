#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<vector<int>> arr) {
    vector<int> answer;

    int n = arr.size();

    vector<vector<int>> copy;

    return answer;
}